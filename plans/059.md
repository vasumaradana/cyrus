# Implementation Plan: Fix verification failures — 022-Write-test-hook

**Issue**: [059](/home/daniel/Projects/barf/cyrus/issues/059.md)
**Created**: 2026-03-18
**PROMPT**: `.claude/agents/` (standard build agent)

## Gap Analysis

**Already exists**:
- Virtual environment at `cyrus2/.venv/` — fully functional (Python 3.13.5, ruff 0.15.6, pytest 9.0.2)
- Issue 065 already resolved the `python: not found` root cause
- `test_hook.py` — 27 tests all passing
- TEST_COMMAND passes (761 tests, 25 subtests)

**Needs building**:
- Fix 6 ruff lint errors (5× E501 line-too-long, 1× F841 unused variable) so BUILD_COMMAND passes
- Fix formatting in `test_030_headless_mode.py` so CHECK_COMMAND passes
- All errors are in 2 files introduced by the headless-mode feature (issue 054), not by 022 itself

## Approach

The original verification failure (`cyrus2/.venv/bin/python: not found`) was resolved by issue 065 which ensured the venv exists. However, the project-wide BUILD_COMMAND and CHECK_COMMAND now fail due to lint/format violations in `cyrus_common.py` and `tests/test_030_headless_mode.py`. These must be fixed for the acceptance criteria to pass.

**Strategy**: Fix line-length violations by wrapping comments/docstrings and reformatting long lines. Fix the unused variable by removing the assignment. Then run `ruff format` to auto-fix formatting.

## Rules to Follow

- `.claude/rules/` — Empty directory, no custom rules defined
- Standard ruff configuration in `cyrus2/pyproject.toml` (max line length = 88)

## Skills & Agents to Use

| Task | Skill/Agent | Purpose |
|------|-------------|---------|
| Fix lint errors | `python-linting` skill | Ensure ruff compliance |
| Fix code style | `python-code-style` skill | Line length, formatting |

## Prioritized Tasks

- [x] Fix `cyrus2/cyrus_common.py:148` — wrap long comment (91 > 88 chars)
- [x] Fix `cyrus2/tests/test_030_headless_mode.py:227` — wrap docstring (89 > 88)
- [x] Fix `cyrus2/tests/test_030_headless_mode.py:500` — break long `patch.object` line (89 > 88)
- [x] Fix `cyrus2/tests/test_030_headless_mode.py:550` — wrap comment (89 > 88)
- [x] Fix `cyrus2/tests/test_030_headless_mode.py:552` — break long `patch.object` line (91 > 88)
- [x] Fix `cyrus2/tests/test_030_headless_mode.py:600` — remove unused `brain` variable (F841)
- [x] Run `ruff format cyrus2/tests/test_030_headless_mode.py` to fix formatting
- [x] Verify BUILD_COMMAND passes (exit 0)
- [x] Verify CHECK_COMMAND passes (exit 0)
- [x] Verify TEST_COMMAND passes (761+ tests) — 892 tests pass

## Acceptance-Driven Tests

| Acceptance Criterion | Required Test | Type |
|---------------------|---------------|------|
| BUILD_COMMAND passes | `cyrus2/.venv/bin/python -m ruff check cyrus2/` exits 0 | lint |
| CHECK_COMMAND passes | `cyrus2/.venv/bin/python -m ruff format --check cyrus2/` exits 0 | format |
| TEST_COMMAND passes | `cyrus2/.venv/bin/python -m pytest cyrus2/tests/` exits 0 | test |

**No cheating** — cannot claim done without all three commands passing.

## Validation (Backpressure)

- Lint: `cyrus2/.venv/bin/python -m ruff check cyrus2/` must exit 0
- Format: `cyrus2/.venv/bin/python -m ruff format --check cyrus2/` must exit 0
- Tests: `cyrus2/.venv/bin/python -m pytest cyrus2/tests/` — 761+ tests pass

## Files to Create/Modify

- `cyrus2/cyrus_common.py` — wrap long comment on line 148
- `cyrus2/tests/test_030_headless_mode.py` — fix 5 E501 violations, 1 F841, reformat
