# Implementation Plan: Fix verification failures for 005-Extract-shared-code-into-cyrus-common

**Issue**: [045](/home/daniel/Projects/barf/cyrus/issues/045.md)
**Created**: 2026-03-16
**Parent**: 005-Extract-shared-code-into-cyrus-common

## Gap Analysis

**Already exists**:
- The venv at `cyrus2/.venv/` is healthy — Python 3.13.5, ruff 0.15.6, pytest 9.0.2 all installed
- `BUILD_COMMAND` (ruff check) passes: "All checks passed!"
- `CHECK_COMMAND` (ruff format --check) passes: "10 files already formatted"
- `TEST_COMMAND` (pytest) passes: "34 passed in 0.03s"
- `cyrus_common.py` extraction from issue 005 is complete and correct

**Root cause of original failure**: The verification ran when `cyrus2/.venv/bin/python` was not available — likely the venv had not been created yet or was broken at the time issue 005 was verified. The venv has since been set up correctly.

**Needs fixing**:
1. Duplicate `TEST_COMMAND=` in `.barfrc` — line 13 has an empty `TEST_COMMAND=` and line 35 has the real one. The empty duplicate on line 13 is a leftover from initial config and should be removed to prevent confusion.
2. Re-verification — the commands need to be confirmed passing for barf to mark the issue COMPLETE.

## Approach

This is a minimal cleanup issue. The verification failures were transient — the venv now exists and all commands pass. The fix is:
1. Clean up the `.barfrc` duplicate `TEST_COMMAND=` entry
2. Let barf re-run verification (which will now pass)

**Why not change the command style?** The current `cyrus2/.venv/bin/python -m <tool>` pattern works correctly and is the standard way to invoke venv-installed tools. Switching to direct binary invocation (`cyrus2/.venv/bin/ruff`) would also work but provides no meaningful benefit — both resolve to the same venv. Keeping `python -m` is more explicit about which Python environment is being used.

## Rules to Follow
- No `.claude/rules/` configured for this project

## Skills & Agents to Use
| Task | Skill/Agent | Purpose |
|------|-------------|---------|
| Edit .barfrc | Direct edit | Remove duplicate TEST_COMMAND line |

## Prioritized Tasks
- [ ] Remove duplicate empty `TEST_COMMAND=` from line 13 of `.barfrc`
- [ ] Run BUILD_COMMAND and confirm it passes
- [ ] Run CHECK_COMMAND and confirm it passes
- [ ] Run TEST_COMMAND and confirm it passes

## Acceptance-Driven Tests
| Acceptance Criterion | Required Test | Type |
|---------------------|---------------|------|
| BUILD_COMMAND passes | `cyrus2/.venv/bin/python -m ruff check cyrus2/` exits 0 | verification |
| CHECK_COMMAND passes | `cyrus2/.venv/bin/python -m ruff format --check cyrus2/` exits 0 | verification |
| TEST_COMMAND passes | `cyrus2/.venv/bin/python -m pytest cyrus2/tests/` exits 0, 34 tests pass | verification |

## Validation (Backpressure)
- Tests: All 34 pytest tests must pass
- Lint: `ruff check cyrus2/` must pass with zero violations
- Format: `ruff format --check cyrus2/` must confirm all files formatted

## Files to Create/Modify
- `/home/daniel/Projects/barf/cyrus/.barfrc` — Remove duplicate empty `TEST_COMMAND=` on line 13
