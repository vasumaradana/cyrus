# Implementation Plan: Fix verification failures for 005-Extract-shared-code-into-cyrus-common

**Issue**: [045](/home/daniel/Projects/barf/cyrus/issues/045.md)
**Created**: 2026-03-16
**Parent**: 005-Extract-shared-code-into-cyrus-common

## Gap Analysis

**Already exists**:
- The venv at `cyrus2/.venv/` is healthy — Python 3.13.5, ruff 0.15.6, pytest 9.0.2 all installed
- `cyrus_common.py` extraction from issue 005 is complete and correct

**Root cause of original failure**: The verification ran when `cyrus2/.venv/bin/python` was not available — likely the venv had not been created yet or was broken at the time issue 005 was verified.

**Actual work required** (discovered during implementation):
- `BUILD_COMMAND` (ruff check) was failing with 9 errors in `test_007_command_handlers.py`:
  - E501 (5 long lines), I001 (unsorted import), F841 (3 unused variables)
- `CHECK_COMMAND` (ruff format --check) was failing — same file needed reformatting
- `TEST_COMMAND` (pytest) was already passing — 109 tests passing
- Duplicate `TEST_COMMAND=` on line 13 of `.barfrc` needed removal
- **Second pass**: `test_008_init_functions.py` introduced 19 new lint errors (18x E501, 1x F841)
  after being added by a subsequent builder — fixed in this pass

**Fixed**:
1. Removed duplicate empty `TEST_COMMAND=` from `.barfrc` line 13
2. Fixed all lint errors in `cyrus2/tests/test_007_command_handlers.py`:
   - Shortened 5 long docstring/comment lines (E501)
   - Reordered `_COMMAND_HANDLERS` before `CommandResult` in import block (I001)
   - Removed 3 unused variable assignments: `result`, `mock_rct`, `tts_calls` (F841)
3. Ran `ruff format` to reformat the file
4. Fixed all lint errors in `cyrus2/tests/test_008_init_functions.py`:
   - Shortened 18 long lines (E501) by splitting long assertions and docstrings
   - Removed unused variable `original_start` (F841) — was leftover debug code

## Approach

Minimal cleanup: fix lint and format errors in `test_007_command_handlers.py` that were
preventing verification from passing, plus remove the duplicate `.barfrc` entry.

## Rules to Follow
- No `.claude/rules/` configured for this project

## Skills & Agents to Use
| Task | Skill/Agent | Purpose |
|------|-------------|---------|
| Edit .barfrc | Direct edit | Remove duplicate TEST_COMMAND line |
| Fix test file | Direct edit | Fix E501/F841/I001 lint errors |

## Prioritized Tasks
- [x] Remove duplicate empty `TEST_COMMAND=` from line 13 of `.barfrc`
- [x] Fix lint errors in `test_007_command_handlers.py` (E501, F841, I001)
- [x] Run `ruff format` to fix formatting
- [x] Run BUILD_COMMAND and confirm it passes
- [x] Run CHECK_COMMAND and confirm it passes
- [x] Run TEST_COMMAND and confirm it passes

## Acceptance-Driven Tests
| Acceptance Criterion | Required Test | Type | Status |
|---------------------|---------------|------|--------|
| BUILD_COMMAND passes | `BUILD_COMMAND` exits 0 | verification | ✅ PASS |
| CHECK_COMMAND passes | `CHECK_COMMAND` exits 0 | verification | ✅ PASS |
| TEST_COMMAND passes | `TEST_COMMAND` exits 0, 145 tests pass | verification | ✅ PASS |

## Validation (Backpressure)
- Tests: All 145 pytest tests pass ✅
- Lint: `ruff check cyrus2/` passes with zero violations ✅
- Format: `ruff format --check cyrus2/` confirms all files formatted ✅

## Files to Create/Modify
- `/home/daniel/Projects/barf/cyrus/.barfrc` — Removed duplicate empty `TEST_COMMAND=` on line 13 ✅
- `/home/daniel/Projects/barf/cyrus/cyrus2/tests/test_007_command_handlers.py` — Fixed 9 lint errors ✅
- `/home/daniel/Projects/barf/cyrus/cyrus2/tests/test_008_init_functions.py` — Fixed 19 lint errors ✅
