# Implementation Plan: Fix verification failures: 030-Add-headless-mode-to-brain

**Issue**: [073](/home/daniel/Projects/barf/cyrus/issues/073.md)
**Created**: 2026-03-18
**PROMPT**: PROMPT_plan

## Gap Analysis
**Already exists**: The test file `cyrus2/tests/test_030_headless_mode.py` exists with all tests. Ruff lint (`ruff check`) passes. Pytest passes.
**Needs building**: Single formatting fix — lines 629-631 have an unnecessarily parenthesized assert message that `ruff format` wants collapsed to one line.

## Approach
Run `ruff format` on the single file, or manually collapse the parenthesized string on lines 629-631. This is a trivial formatting-only fix — no logic changes.

The diff is:
```diff
-        assert "CYRUS_HEADLESS" in content, (
-            ".env.example must document CYRUS_HEADLESS"
-        )
+        assert "CYRUS_HEADLESS" in content, ".env.example must document CYRUS_HEADLESS"
```

## Rules to Follow
- No `.claude/rules/` directory exists in this project

## Skills & Agents to Use
| Task | Skill/Agent | Purpose |
|------|-------------|---------|
| Format fix | `python-linting` | Ruff format guidance |

## Prioritized Tasks
- [x] Apply `ruff format` to `cyrus2/tests/test_030_headless_mode.py`
- [x] Verify `ruff format --check cyrus2/` passes
- [x] Verify `ruff check cyrus2/` passes
- [x] Verify `pytest cyrus2/tests/` passes

## Acceptance-Driven Tests
| Acceptance Criterion | Required Test | Type |
|---------------------|---------------|------|
| `ruff check cyrus2/` passes | Run `cyrus2/.venv/bin/python -m ruff check cyrus2/` | lint |
| `ruff format --check cyrus2/` passes | Run `cyrus2/.venv/bin/python -m ruff format --check cyrus2/` | format |
| `pytest cyrus2/tests/` passes | Run `cyrus2/.venv/bin/python -m pytest cyrus2/tests/` | test |

## Validation (Backpressure)
- Tests: `cyrus2/.venv/bin/python -m pytest cyrus2/tests/` must pass
- Lint: `cyrus2/.venv/bin/python -m ruff check cyrus2/` must pass
- Format: `cyrus2/.venv/bin/python -m ruff format --check cyrus2/` must pass

## Files to Create/Modify
- `cyrus2/tests/test_030_headless_mode.py` - Collapse parenthesized assert message on line 629
