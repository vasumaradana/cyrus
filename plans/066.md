# Verification: Fix repeated verification failure: build — Command not found: cyrus2/.venv/bin/python

**Issue**: [066](/home/daniel/Projects/barf/cyrus/issues/066.md)
**Status**: COMPLETE
**Created**: 2026-03-18

## Evidence

This is a **duplicate of the same systemic barf-ts infrastructure bug** documented in plans 061, 063, 064, and 065.

**Root cause**: barf-ts was not passing `projectCwd` through the verification pipeline, so relative paths like `cyrus2/.venv/bin/python` in `.barfrc` were resolved against barf's process directory instead of the cyrus project root (`/home/daniel/Projects/barf/cyrus/`).

**Fix already applied in barf-ts** (commit `edb54f7`):
- `packages/server/src/workers/barf-worker.ts` — passes `projectCwd: projectDir`
- `packages/core/src/core/batch/loop.ts` — `RunLoopDeps` type includes `projectCwd`
- `packages/core/src/core/batch/outcomes.ts` — forwards `projectCwd` to `verifyIssue`
- `packages/core/src/core/verification/orchestration.ts` — accepts `cwd` parameter
- `packages/core/src/core/verification/checks.ts` — passes `cwd` to exec
- `packages/core/src/utils/execFileNoThrow.ts` — spawns with `cwd` option

**All cyrus verification commands pass from the project directory:**
- `BUILD_COMMAND`: `cyrus2/.venv/bin/python -m ruff check cyrus2/` — All checks passed
- `CHECK_COMMAND`: `cyrus2/.venv/bin/python -m ruff format --check cyrus2/` — 38 files already formatted
- `TEST_COMMAND`: `cyrus2/.venv/bin/python -m pytest cyrus2/tests/` — 761 passed in 14.69s

**Related plans confirming the same fix:**
- Plan 061 — ALREADY IMPLEMENTED
- Plan 063 — ALREADY IMPLEMENTED
- Plan 064 — ALREADY IMPLEMENTED
- Issue 065 — marked COMPLETE (2026-03-18 16:58)

## Verification Steps
- [x] `BUILD_COMMAND` passes: `cyrus2/.venv/bin/python -m ruff check cyrus2/` — All checks passed
- [x] `CHECK_COMMAND` passes: `cyrus2/.venv/bin/python -m ruff format --check cyrus2/` — 38 files already formatted
- [x] `TEST_COMMAND` passes: `cyrus2/.venv/bin/python -m pytest cyrus2/tests/` — 761 passed
- [x] barf-ts fix confirmed: `projectCwd` threaded through verification pipeline (commit `edb54f7`)

## Minor Fixes Needed (if any)
- None. The root cause was in barf-ts and has been fixed.

## Recommendation
Mark issue complete after running verification steps. No changes needed in the cyrus project.
