# Implementation Plan: Fix verification failures for 065

**Issue**: [071](/home/daniel/Projects/barf/cyrus/issues/071.md)
**Created**: 2026-03-18
**PROMPT**: PROMPT_plan

## Gap Analysis
**Already exists**: Both test files exist and have correct test logic
**Needs building**: Fix lint/format violations only — no logic changes

## Approach
Apply targeted fixes to two test files to resolve ruff lint errors and ruff format issues. All fixes are mechanical (formatting, import sorting, line length, modernizing exception types). No behavioral changes.

## Rules to Follow
- No relevant custom rules in `.claude/rules/` (directory is empty)

## Skills & Agents to Use
| Task | Skill/Agent | Purpose |
|------|-------------|---------|
| Fix lint errors | Direct edits | Simple mechanical fixes |

## Prioritized Tasks
- [x] Fix `test_029_brain_host_configurable.py` — 5 ruff errors:
  - I001: Added `# noqa: E402,I001` to `import cyrus_hook` line
  - E501 x3 (lines 158, 179, 200): Broke long `with patch(...)` lines to ≤88 chars
  - UP041 (line 260): Replaced `socket_mod.timeout("timed out")` with `TimeoutError("timed out")`
- [x] Fix `test_028_tcp_authentication.py` — ruff format only (ran `ruff format`)
- [x] Verify: `ruff check cyrus2/tests/test_029_brain_host_configurable.py` passes
- [x] Verify: `ruff format --check cyrus2/tests/test_028_tcp_authentication.py cyrus2/tests/test_029_brain_host_configurable.py` passes
- [x] Verify: `pytest cyrus2/tests/test_028_tcp_authentication.py cyrus2/tests/test_029_brain_host_configurable.py` passes — 41 passed

## Acceptance-Driven Tests
| Acceptance Criterion | Required Test | Type |
|---------------------|---------------|------|
| `BUILD_COMMAND` passes | lint: 0 errors | lint |
| `CHECK_COMMAND` passes | format: 0 files need reformatting | format |
| `TEST_COMMAND` passes | all tests green | unit |

**No cheating** - cannot claim done without required tests passing.

## Validation (Backpressure)
- Lint: `ruff check cyrus2/tests/test_029_brain_host_configurable.py` — 0 errors
- Format: `ruff format --check cyrus2/tests/test_028_tcp_authentication.py cyrus2/tests/test_029_brain_host_configurable.py` — already formatted
- Tests: Both test files importable and passing

## Files to Create/Modify
- `cyrus2/tests/test_029_brain_host_configurable.py` — fix I001, E501 x3, UP041
- `cyrus2/tests/test_028_tcp_authentication.py` — ruff format only

## Specific Fixes

### test_029 — I001 (import sorting)
The `import cyrus_hook` on line 33 needs to be in a separate `# noqa: E402` block after the sys.path manipulation. The I001 error means ruff wants imports sorted — likely need to add `# isort: skip` or restructure. Since the import MUST come after `sys.path.insert`, use `# noqa: I001` or restructure with `isort:skip`.

### test_029 — E501 (lines 158, 179, 200)
All three are identical pattern:
```python
with patch("cyrus_hook.socket.create_connection", return_value=mock_socket) as mock_conn:
```
Fix: break across lines:
```python
with patch(
    "cyrus_hook.socket.create_connection",
    return_value=mock_socket,
) as mock_conn:
```

### test_029 — UP041 (line 260)
Replace `socket_mod.timeout("timed out")` → `TimeoutError("timed out")` and remove `import socket as socket_mod`.

### test_028 — format
Run `ruff format` on the file. No manual changes needed.
