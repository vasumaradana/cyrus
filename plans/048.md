# Implementation Plan: Fix verification failures — 011-Replace-prints-in-cyrus-voice

**Issue**: [048](/home/daniel/Projects/barf/cyrus/issues/048.md)
**Created**: 2026-03-16 (updated 2026-03-16)
**PROMPT**: `prompts/PROMPT_plan.md`

## Gap Analysis

**Already exists** (issue 011's core work is complete):
- `cyrus_brain.py` — 0 `print()` calls remain (all 44 replaced with `log.*()`)
- `from cyrus_log import setup_logging` imported (line 96)
- `import logging` present (line 39 area, within `from dataclasses import dataclass` block)
- `log = logging.getLogger("cyrus.brain")` defined at module level (line 99)
- `setup_logging("cyrus")` called in `main()`
- 60 `log.*()` calls throughout at correct levels (debug/info/warning/error)
- All 226 tests pass ✅ (including `test_cache_clear_retry_uses_warning` which was updated to accept `.warning(` pattern)

**Needs building** (lint/format only — 1 file):

### Fix: E501 violations and formatting in test file

`test_010_print_replacement.py` has **5 lines** exceeding the 88-character limit (ruff E501) and needs `ruff format` applied:

| Line | Length | Content |
|------|--------|---------|
| 110 | 96 | Comment: `# Accept both 'from cyrus_log import ...' and 'from cyrus2.cyrus_log import ...'` |
| 273 | 91 | Docstring: `"""Companion extension unavailable / falling back to UIA must use log.warning()."""` |
| 443 | 91 | f-string: `f"Expected ≥4 log.error() calls in except blocks, found {len(calls)}: {calls}"` |
| 598 | 90 | String: `"result.log_message must be forwarded via log.info('%s', result.log_message)"` |
| 631 | 93 | String: `"FATAL UIAutomation error message not found in source"` (with assertTrue prefix) |

### NOT needed (previously planned, now resolved):

~~Fix 1: Move `log = logging.getLogger("cyrus.brain")` earlier~~ — The test `test_cache_clear_retry_uses_warning` was updated (lines 290-307) to check for `.warning(` in a 3-line region, which matches both `log.warning(` and `logging.getLogger("cyrus.brain").warning(`. All 226 tests pass. The `logging.getLogger()` calls in the import recovery block (lines 63, 69) work correctly and the tests accept them.

## Approach

**Single-file lint fix.** Shorten 5 long lines in `test_010_print_replacement.py` to ≤88 characters, then run `ruff format` to ensure consistent formatting.

**Why**: The only remaining verification failures are lint (5 E501) and format (1 file). Tests all pass. No source file changes needed — only the test file.

**Line-by-line fix strategy**:

1. **Line 110** (96 chars, comment): Split comment across two lines
   ```python
   # Accept both 'from cyrus_log import ...'
   # and 'from cyrus2.cyrus_log import ...'
   ```

2. **Line 273** (91 chars, docstring): Wrap to multi-line docstring
   ```python
   """Companion extension unavailable / falling back to
   UIA must use log.warning()."""
   ```

3. **Line 443** (91 chars, f-string): Split assertion message
   ```python
   (
       f"Expected ≥4 log.error() calls in except blocks, "
       f"found {len(calls)}: {calls}"
   ),
   ```

4. **Line 598** (90 chars, string): Split assertion message
   ```python
   "result.log_message must be forwarded via "
   "log.info('%s', result.log_message)",
   ```

5. **Line 631** (93 chars, string): Split assertion message
   ```python
   self.assertTrue(
       fatal_region,
       "FATAL UIAutomation error message not found in source",
   )
   ```

After manual fixes, run `ruff format` to ensure consistent formatting.

## Rules to Follow

- No custom rules in `.claude/rules/` — follow `pyproject.toml` strictly
- Line length: 88 characters (ruff, configured in `cyrus2/pyproject.toml`)
- Lint rules: E, F, W, I, UP, B (ruff)
- Python target: 3.10+

## Skills & Agents to Use

| Task | Skill/Agent | Purpose |
|------|-------------|---------|
| Fix E501 + format | `python-linting` skill | Ruff check/format commands |

## Prioritized Tasks

- [x] Fix E501 on line 110 — split long comment across two lines
- [x] Fix E501 on line 273 — wrap docstring to multi-line
- [x] Fix E501 on line 443 — split f-string assertion message
- [x] Fix E501 on line 598 — split string literal assertion message
- [x] Fix E501 on line 631 — reformat assertTrue call with wrapped message
- [x] Run `ruff format cyrus2/tests/test_010_print_replacement.py`
- [x] Run verification: `ruff check cyrus2/`, `ruff format --check cyrus2/`, `pytest cyrus2/tests/`

## Acceptance-Driven Tests

| Acceptance Criterion | Required Test | Type |
|---------------------|---------------|------|
| `ruff check` passes | `BUILD_COMMAND`: `BUILD_COMMAND` | lint |
| `ruff format --check` passes | `CHECK_COMMAND`: `CHECK_COMMAND` | format |
| All pytest tests pass | `TEST_COMMAND`: `TEST_COMMAND` | unit |

**No cheating** — cannot claim done without all 3 verification commands passing.

## Validation (Backpressure)

- **Build**: `ruff check cyrus2/` — ✅ 0 errors ("All checks passed!")
- **Check**: `ruff format --check cyrus2/` — ✅ 0 reformats (38 files already formatted)
- **Test**: `pytest cyrus2/tests/` — ✅ 761 passed (0 failures)

## Files to Create/Modify

- `cyrus2/tests/test_010_print_replacement.py` — Fix 5 E501 line-length violations, run ruff format
