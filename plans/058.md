# Implementation Plan: Fix verification failures: 021-Write-test-fast-command

**Issue**: [058](/home/daniel/Projects/barf/cyrus/issues/058.md)
**Created**: 2026-03-18
**PROMPT**: prompts/PROMPT_plan.md

## Gap Analysis
**Already exists**:
- Virtual environment at `cyrus2/.venv/bin/python` exists and is functional (Python 3.13.5)
- All 761 tests pass (`TEST_COMMAND` succeeds)
- The venv has ruff and pytest installed
- The original "not found" error is resolved — the venv exists and commands execute

**Needs fixing**:
- `BUILD_COMMAND` fails with 6 ruff errors:
  - `cyrus2/cyrus_common.py:148` — E501 line too long (91 > 88), comment line
  - `cyrus2/tests/test_030_headless_mode.py:227` — E501 (89 > 88), docstring line
  - `cyrus2/tests/test_030_headless_mode.py:500` — E501 (89 > 88), `with patch.object(...)` line
  - `cyrus2/tests/test_030_headless_mode.py:550` — E501 (89 > 88), comment line
  - `cyrus2/tests/test_030_headless_mode.py:552` — E501 (91 > 88), `with patch.object(...)` line
  - `cyrus2/tests/test_030_headless_mode.py:600` — F841 unused variable `brain`
- `CHECK_COMMAND` fails: `cyrus2/tests/test_030_headless_mode.py` needs reformatting

## Approach
Fix all 6 ruff violations (5 E501 line-too-long + 1 F841 unused variable) and run `ruff format` on the affected file. These are straightforward text wrapping and unused variable fixes. The original "cyrus2/.venv/bin/python: not found" error was a transient issue (venv now exists and works).

**Why this approach**: Direct line-level fixes are the simplest path. The E501 errors just need line wrapping. The F841 needs the unused variable removed or prefixed with `_`. Running `ruff format` after manual fixes will handle any remaining formatting issues.

**Note**: Issue 054 targets the same root cause. These are sibling fix issues for different parent issues (054 for #017, 058 for #021) that also affect #022, #023, #024, #025, #065, #010.

## Rules to Follow
- No `.claude/rules/` files exist in this project

## Skills & Agents to Use
| Task | Skill/Agent | Purpose |
|------|-------------|---------|
| Fix lint errors | `python-linting` skill | Ruff line-length compliance |
| Fix code style | `python-code-style` skill | Formatting compliance |

## Prioritized Tasks
- [x] Fix E501 in `cyrus2/cyrus_common.py:148` — wrap comment to ≤88 chars
- [x] Fix E501 in `cyrus2/tests/test_030_headless_mode.py:227` — wrap docstring to ≤88 chars
- [x] Fix E501 in `cyrus2/tests/test_030_headless_mode.py:500` — break `with patch.object(...)` across lines
- [x] Fix E501 in `cyrus2/tests/test_030_headless_mode.py:550` — wrap comment to ≤88 chars
- [x] Fix E501 in `cyrus2/tests/test_030_headless_mode.py:552` — break `with patch.object(...)` across lines
- [x] Fix F841 in `cyrus2/tests/test_030_headless_mode.py:600` — remove or prefix unused `brain` variable
- [x] Run `ruff format` on `cyrus2/tests/test_030_headless_mode.py`
- [x] Verify BUILD_COMMAND passes (`ruff check` exits 0) — PASSES: "All checks passed!"
- [x] Verify CHECK_COMMAND passes (`ruff format --check` exits 0) — PASSES: "43 files already formatted"
- [x] Verify TEST_COMMAND still passes (761 tests) — PASSES: 892 tests passed

## Acceptance-Driven Tests
| Acceptance Criterion | Required Test | Type |
|---------------------|---------------|------|
| BUILD_COMMAND passes | `cyrus2/.venv/bin/python -m ruff check cyrus2/` exits 0 | lint |
| CHECK_COMMAND passes | `cyrus2/.venv/bin/python -m ruff format --check cyrus2/` exits 0 | format |
| TEST_COMMAND passes | `cyrus2/.venv/bin/python -m pytest cyrus2/tests/` exits 0 | test |

**No cheating** - cannot claim done without all three commands passing.

## Validation (Backpressure)
- Tests: 761 tests must continue passing
- Lint: `ruff check` must exit 0
- Build: `ruff format --check` must exit 0

## Files to Create/Modify
- `cyrus2/cyrus_common.py` — wrap comment on line 148
- `cyrus2/tests/test_030_headless_mode.py` — fix 4 E501 + 1 F841 + autoformat
