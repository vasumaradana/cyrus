# Implementation Plan: Fix verification failures for 023-Write-test-permission-keywords

**Issue**: [060](/home/daniel/Projects/barf/cyrus/issues/060.md)
**Created**: 2026-03-18
**PROMPT**: PROMPT_plan

## Gap Analysis
**Already exists**:
- Virtual environment at `cyrus2/.venv/` is present and functional (Python 3.13.5, ruff, pytest installed)
- Issue 065 (COMPLETE) fixed the systemic `cyrus2/.venv/bin/python: not found` error that was blocking verification across all child issues including 060
- `BUILD_COMMAND` (`ruff check`) now passes cleanly
- `TEST_COMMAND` (`pytest`) passes — 761 tests including all test_permission_keywords.py tests
- test_permission_keywords.py is fully implemented with 48 parametrized tests

**Needs building**:
- `CHECK_COMMAND` (`ruff format --check`) fails on ONE file: `cyrus2/tests/test_030_headless_mode.py` — needs auto-formatting
- This is the only remaining blocker for all three acceptance criteria to pass

## Approach
The original verification failure (python not found) was a systemic issue already fixed by issue 065. The only remaining gap is a formatting issue in an unrelated test file (`test_030_headless_mode.py`) that causes `CHECK_COMMAND` to fail globally. Fix: run `ruff format` on that file.

**Why this approach**: The formatting fix is trivial and mechanical (auto-formatter). No logic changes needed. All actual test functionality for issue 023 is already working.

## Rules to Follow
- No `.claude/rules/` files exist in this project (directory is empty)

## Skills & Agents to Use
| Task | Skill/Agent | Purpose |
|------|-------------|---------|
| Format fix | Direct edit | Single `ruff format` command — no agent needed |

## Prioritized Tasks
- [x] Run `cyrus2/.venv/bin/python -m ruff format cyrus2/tests/test_030_headless_mode.py` to auto-fix formatting
- [x] Verify `BUILD_COMMAND` passes: `cyrus2/.venv/bin/python -m ruff check cyrus2/`
- [x] Verify `CHECK_COMMAND` passes: `cyrus2/.venv/bin/python -m ruff format --check cyrus2/`
- [x] Verify `TEST_COMMAND` passes: `cyrus2/.venv/bin/python -m pytest cyrus2/tests/`

## Acceptance-Driven Tests
| Acceptance Criterion | Required Test | Type |
|---------------------|---------------|------|
| BUILD_COMMAND passes | `cyrus2/.venv/bin/python -m ruff check cyrus2/` exits 0 | lint |
| CHECK_COMMAND passes | `cyrus2/.venv/bin/python -m ruff format --check cyrus2/` exits 0 | format |
| TEST_COMMAND passes | `cyrus2/.venv/bin/python -m pytest cyrus2/tests/` exits 0 | unit/integration |

**No cheating** - cannot claim done without all three commands passing with exit code 0.

## Validation (Backpressure)
- Tests: 761 tests must pass (`pytest cyrus2/tests/`)
- Lint: `ruff check cyrus2/` must pass
- Build: `ruff format --check cyrus2/` must pass

## Files to Create/Modify
- `cyrus2/tests/test_030_headless_mode.py` — auto-format with ruff (no logic changes)
