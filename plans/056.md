# Implementation Plan: Fix verification failures (056)

**Issue**: [056](/home/daniel/Projects/barf/cyrus/issues/056.md)
**Created**: 2026-03-18
**PROMPT**: prompts/PROMPT_build.md

## Gap Analysis

**Already exists**:
- Virtual environment at `cyrus2/.venv/` with Python 3.13.5, ruff, pytest installed
- `.barfrc` commands use `cyrus2/.venv/bin/python` which resolves correctly from `cyrus/` working directory
- All 761 tests pass (`TEST_COMMAND` succeeds)
- The original "python not found" error is no longer reproducible - the venv exists and works

**Needs building**:
- Fix 5 ruff lint errors (E501 line too long, F841 unused variable) so `BUILD_COMMAND` passes
- Fix 1 ruff format violation so `CHECK_COMMAND` passes

## Approach

The original verification failure (`sh: 1: cyrus2/.venv/bin/python: not found`) was a transient infrastructure issue - the venv now exists and all commands execute. However, BUILD_COMMAND and CHECK_COMMAND still fail due to lint/format violations in `cyrus_common.py` and `test_030_headless_mode.py`. Fix these code quality issues to make all three acceptance criteria pass.

**Why this approach**: The acceptance criteria require all three commands to pass. The venv issue is resolved; only lint/format fixes remain.

## Rules to Follow
- No `.claude/rules/` directory exists for this project
- Follow existing code style (88 char line length per pyproject.toml ruff config)

## Skills & Agents to Use
| Task | Skill/Agent | Purpose |
|------|-------------|---------|
| Fix lint errors | Direct edit | Simple line-length and unused-var fixes |
| Run verification | Bash | Confirm all 3 commands pass |

## Prioritized Tasks

- [x] Fix E501 in `cyrus2/cyrus_common.py:148` - wrap long comment line (91 > 88 chars)
- [x] Fix E501 in `cyrus2/tests/test_030_headless_mode.py:227` - wrap long docstring (89 > 88 chars)
- [x] Fix E501 in `cyrus2/tests/test_030_headless_mode.py:500` - wrap long `patch.object` line (89 > 88 chars)
- [x] Fix E501 in `cyrus2/tests/test_030_headless_mode.py:550` - wrap long comment (89 > 88 chars)
- [x] Fix E501 in `cyrus2/tests/test_030_headless_mode.py:552` - wrap long `patch.object` line (91 > 88 chars)
- [x] Fix F841 in `cyrus2/tests/test_030_headless_mode.py:600` - remove unused `brain` variable
- [x] Run `ruff format` on `cyrus2/tests/test_030_headless_mode.py` to fix formatting
- [x] Verify BUILD_COMMAND passes (exit 0)
- [x] Verify CHECK_COMMAND passes (exit 0)
- [x] Verify TEST_COMMAND passes (exit 0)

## Acceptance-Driven Tests
| Acceptance Criterion | Required Test | Type |
|---------------------|---------------|------|
| BUILD_COMMAND passes | `cyrus2/.venv/bin/python -m ruff check cyrus2/` exits 0 | lint |
| CHECK_COMMAND passes | `cyrus2/.venv/bin/python -m ruff format --check cyrus2/` exits 0 | format |
| TEST_COMMAND passes | `cyrus2/.venv/bin/python -m pytest cyrus2/tests/` exits 0 | test |

**No cheating** - cannot claim done without all three commands returning exit code 0.

## Validation (Backpressure)
- Tests: 761 existing tests must still pass
- Lint: `ruff check cyrus2/` must exit 0
- Format: `ruff format --check cyrus2/` must exit 0

## Files to Create/Modify
- `cyrus2/cyrus_common.py` - Fix 1 long comment line (line 148)
- `cyrus2/tests/test_030_headless_mode.py` - Fix 5 lint errors + format issues
