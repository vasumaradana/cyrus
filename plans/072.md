# Implementation Plan: Fix verification failures: 065

**Issue**: [072](/home/daniel/Projects/barf/cyrus/issues/072.md)
**Created**: 2026-03-18
**PROMPT**: Fix lint (ruff) and format (ruff format) errors in two test files

## Gap Analysis
**Already exists**: Test files test_028 and test_029 are fully written with correct logic
**Needs building**: Fix 5 ruff lint errors in test_029 + reformat both test_028 and test_029

## Approach
Run `ruff check --fix` and `ruff format` on the two affected files. For the E501 lines that can't be auto-fixed, manually break the long `with patch(...)` lines. The fixes are mechanical - no logic changes needed.

## Rules to Follow
- No rules in `.claude/rules/` (directory is empty)

## Skills & Agents to Use
| Task | Skill/Agent | Purpose |
|------|-------------|---------|
| Lint fix | `python-linting` skill | Reference for ruff conventions |

## Prioritized Tasks
- [x] Fix I001: import sorting in test_029 (line 33) - `ruff check --fix` handles this
- [x] Fix E501: three lines >88 chars in test_029 (lines 158, 179, 200) - manually break `with patch(...)` lines
- [x] Fix UP041: replace `socket_mod.timeout` with `TimeoutError` in test_029 (line 260) - `ruff check --fix` handles this
- [x] Run `ruff format` on test_028 and test_029 to fix formatting
- [x] Verify: `ruff check cyrus2/tests/test_029_brain_host_configurable.py` clean
- [x] Verify: `ruff format --check cyrus2/tests/test_028_tcp_authentication.py cyrus2/tests/test_029_brain_host_configurable.py` clean
- [x] Verify: `pytest cyrus2/tests/test_028_tcp_authentication.py cyrus2/tests/test_029_brain_host_configurable.py` passes

## Acceptance-Driven Tests
| Acceptance Criterion | Required Test | Type |
|---------------------|---------------|------|
| `BUILD_COMMANDtests/` passes | 0 errors | lint |
| `CHECK_COMMANDtests/` passes | no reformats needed | format |
| `TEST_COMMANDtest_028* cyrus2/tests/test_029*` passes | all tests green | unit |

## Validation (Backpressure)
- Tests: pytest on affected test files must pass
- Lint: `ruff check` must report 0 errors on affected files
- Format: `ruff format --check` must report 0 reformats on affected files

## Files to Create/Modify
- `cyrus2/tests/test_029_brain_host_configurable.py` - fix 5 lint errors + reformat
- `cyrus2/tests/test_028_tcp_authentication.py` - reformat only
