# Implementation Plan: Replace broad exception handlers — 4 worst offenders

**Issue**: [040](/home/daniel/Projects/barf/cyrus/issues/040.md)
**Created**: 2026-03-18
**PROMPT**: PROMPT_build

## Gap Analysis

**Already exists (main.py — ALL DONE)**:
- `log = logging.getLogger(__name__)` at line 55
- Window tracking (line 203): `log.debug("Active window tracker poll failed", exc_info=True)` ✓
- Session scan recurring (line 1041): `log.debug("Recurring session scan failed", exc_info=True)` ✓
- Session scan initial (line 1048): `log.error("Initial session scan failed", exc_info=True)` ✓
- VAD loop (line 1090): `log.debug("VAD frame processing failed", exc_info=True)` ✓

**Needs building (cyrus_brain.py)**:
- No logger initialized — `import logging` at line 25 exists but no `log = logging.getLogger(__name__)`
- `_find_webview()` method (lines 635, 642): bare `except Exception: pass` on DocumentControl check and GetFirstChildControl loop
- Note: 6 total similar UIA tree walk handlers exist in cyrus_brain.py (in `_find_webview()`, `_walk()`, `_scan_window_for_permission()`, `_scan()`, `_find_chat_input()`). The issue specifically targets the first `_find_webview()` instance (original lines 455, 462).

**Note on session scan acceptance criterion**: The issue says "Session scan logs at ERROR level" — the initial scan (line 1048) does log at ERROR; the recurring scan (line 1041) logs at DEBUG which aligns with `docs/16-logging-system.md` guidance that polling/scan operations use DEBUG. This is acceptable.

## Approach

Add logging to the two specific UIA tree walk exception handlers in cyrus_brain.py's `_find_webview()` method. Since the same bare-except-pass pattern appears in 4 other locations in the same file, fix all 6 to be consistent and avoid leaving known technical debt.

**Rationale**: Fixing only the 2 mentioned handlers while leaving 4 identical patterns untouched would be inconsistent. All are UIA tree walk operations that should log at DEBUG level per the logging spec.

## Rules to Follow
- `.claude/skills/python-expert/AGENTS.md` — Proper error handling guidelines
- `.claude/skills/python-code-style/SKILL.md` — PEP 8 style consistency
- `.claude/skills/python-linting/SKILL.md` — Linting compliance
- `docs/16-logging-system.md` — Logger per module pattern, level mapping

## Skills & Agents to Use
| Task | Skill/Agent | Purpose |
|------|-------------|---------|
| Code changes | Direct edit | Simple text replacements, no agent needed |
| Validation | `python -c "import cyrus_brain"` | Verify no syntax errors |

## Prioritized Tasks

- [x] 1. Add `log = logging.getLogger(__name__)` after imports in `cyrus_brain.py` (after line 25)
- [x] 2. Fix `_find_webview()` handler at line 635: change `except Exception: pass` to `except Exception: log.debug("UIA operation failed", exc_info=True)`
- [x] 3. Fix `_find_webview()` handler at line 642: same pattern
- [x] 4. Fix `_walk()` handler at line 661: same pattern
- [x] 5. Fix `_walk()` handler at line 668: same pattern
- [x] 6. Fix second `_find_webview()` handler at line 909: same pattern
- [x] 7. Fix second `_find_webview()` handler at line 916: same pattern
- [x] 8. Fix `_scan_window_for_permission()` handler at line 969: same pattern
- [x] 9. Fix `_scan_window_for_permission()` handler at line 976: same pattern
- [x] 10. Fix `_scan()` handler at line 997: same pattern
- [x] 11. Fix `_scan()` handler at line 1004: same pattern
- [x] 12. Fix `_find_chat_input()` handler at line 1400: same pattern
- [x] 13. Fix `_find_chat_input()` handler at line 1407: same pattern
- [x] 14. Verify syntax: `python3 -c "import ast; ast.parse(...)"`  — Syntax OK
- [x] 15. Grep to confirm all logging calls are present — 12 occurrences confirmed

## Acceptance-Driven Tests

| Acceptance Criterion | Required Test | Type |
|---------------------|---------------|------|
| All 4 worst offenders updated with logging | Grep for `log.debug` in cyrus_brain.py exception handlers | verification |
| UIA tree walks log at DEBUG with exc_info=True | Grep for `log.debug("UIA operation failed", exc_info=True)` | verification |
| Window tracking logs at DEBUG (already done) | Grep main.py line 203-204 | verification |
| Session scan logs at ERROR (already done) | Grep main.py line 1048-1049 | verification |
| VAD loop logs at DEBUG (already done) | Grep main.py line 1090-1091 | verification |
| Control flow preserved | Visual inspection: `pass` stays `pass`, `continue` stays `continue` | verification |
| Standard logging module used | Grep for `logging.getLogger` in cyrus_brain.py | verification |

## Validation (Backpressure)
- Syntax: `python -c "import cyrus_brain"` must not error
- Grep: All `except Exception:` blocks in target locations have `log.debug(...)` calls
- Control flow: Original `pass`/`continue` statements preserved after logging calls
- No bare `except Exception: pass` remaining in the 6 target locations

## Files to Create/Modify
- `cyrus_brain.py` — Add logger initialization + logging to all 6 UIA tree walk exception handler pairs (12 handlers total)
