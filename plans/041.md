# Implementation Plan: Replace broad exception handlers — cyrus_brain.py remaining

**Issue**: [041](/home/daniel/Projects/barf/cyrus/issues/041.md)
**Created**: 2026-03-18
**PROMPT**: `/home/daniel/Projects/barf/cyrus/prompts/PROMPT_plan.md`

## Gap Analysis

**Already exists**:
- `import logging` on line 25 of `cyrus_brain.py`
- 1 handler (line 541) already uses `logging.exception()` — pattern reference
- Categorization rules documented in `docs/16-logging-system.md`
- Issue 040 plan exists (establishes 4-worst-offenders pattern) but is not yet built

**Needs building**:
- `log = logging.getLogger(__name__)` initialization (missing)
- ~43 remaining `except Exception` handlers need logging added
- Each handler must be categorized as ERROR, WARNING, or DEBUG
- Print-based error reporting (~9 handlers) must be converted to log calls
- Silent `pass` handlers (~16 tree-walk, ~4 cleanup, ~10 state recovery) need log calls

## Approach

**Why this approach**: The issue specifies a mechanical transformation — examine each handler's try-block context, categorize by severity, add `log.LEVEL("context", exc_info=True)`, and preserve control flow. No architectural changes, just systematic logging addition.

**Dependency note**: Issue 040 (4 worst offenders) is PLANNED but not built. This issue targets different handlers (the remaining ~41), so we can proceed independently. If 040 has already modified some handlers by build time, we skip those.

**Categorization strategy** (from docs/16-logging-system.md):

| Category | Level | Handlers (approx) | Examples |
|----------|-------|--------------------|----------|
| UI tree walks / control traversal | DEBUG | ~16 | Lines 635, 642, 661, 668, 909, 916, 969, 976, 997, 1004, 1400, 1407 |
| Resource cleanup (close/drain) | DEBUG | ~4 | Lines 296, 1936, 1980, 1572 |
| Window focus/activation | DEBUG | ~5 | Lines 1111, 1132, 1544, 1564, 1555 |
| Polling recovery loops | WARNING | ~4 | Lines 761, 826, 1260, 1523 |
| Network/WebSocket failures | WARNING | ~3 | Lines 247, 265, 1474 |
| Window discovery/tracking | DEBUG | ~3 | Lines 1335, 1342, 1371 |
| Import guards | WARNING | ~2 | Lines 45, 63 |
| Coord caching | DEBUG | ~2 | Lines 752, 1021, 1511 |
| Input/prompt errors | ERROR | ~1 | Line 1166 |
| Hook handler errors | ERROR | ~1 | Line 1930 |
| Voice/mobile client errors | ERROR | ~2 | Lines 1633, 1659 |
| Submit worker errors | ERROR | ~1 | Line 1594 |
| Permission scan fallback | WARNING | ~1 | Line 981 |
| Permission button click | WARNING | ~1 | Line 1119 |

## Rules to Follow
- No custom `.claude/rules/` files exist — follow standard project conventions
- `pyproject.toml` linting: ruff with E, F, W, I, UP, B rules, line length 88, Python 3.10+

## Skills & Agents to Use

| Task | Skill/Agent | Purpose |
|------|-------------|---------|
| Exception handler refactoring | `python-patterns` skill | EAFP patterns, error handling best practices |
| Code quality check | `python-linting` skill | Run ruff after changes |
| Post-change review | `code-reviewer` agent | Verify categorization correctness |

## Prioritized Tasks

- [x] 1. Add `log = logging.getLogger(__name__)` after `import logging` (line 25)
- [x] 2. Convert DEBUG-level handlers (~25 handlers): UI tree walks, resource cleanup, window focus, coord caching, window discovery
- [x] 3. Convert WARNING-level handlers (~8 handlers): polling recovery, network failures, import guards, permission scan/click
- [x] 4. Convert ERROR-level handlers (~5 handlers): prompt input, hook handler, voice/mobile client, submit worker
- [x] 5. Convert the existing `logging.exception()` call (line 541) to use `log.exception()` for consistency
- [x] 6. Remove/replace all `print(f"[Brain]...")` statements in except blocks with `log.LEVEL()` calls
- [x] 7. Run `ruff check cyrus_brain.py` and fix any issues
- [x] 8. Verify no `except Exception` handler remains without a log call

## Acceptance-Driven Tests

| Acceptance Criterion | Required Test | Type |
|---------------------|---------------|------|
| All ~41 remaining handlers have logging | `grep -c "except Exception" cyrus_brain.py` matches count of `log.` calls in except blocks | verification |
| Handlers categorized correctly | Manual review: tree walks=DEBUG, network=WARNING/ERROR, cleanup=DEBUG | review |
| All use `exc_info=True` | `grep "exc_info=True" cyrus_brain.py` count matches handler count | verification |
| Control flow preserved | Verify `continue`, `return`, `break` statements remain after log calls | review |
| File has logger initialization | `grep "log = logging.getLogger" cyrus_brain.py` returns match | verification |
| No silent failures in critical paths | Verify ERROR-level handlers cover network, hook, voice, submit paths | review |

**No cheating** — cannot claim done without all verification checks passing.

## Validation (Backpressure)

- **Lint**: `ruff check cyrus_brain.py` must pass clean
- **Grep verification**: Every `except Exception` block must contain a `log.` call
- **Control flow**: No behavioral changes — only logging additions
- **Syntax**: `python -m py_compile cyrus_brain.py` must succeed

## Files to Create/Modify

- `cyrus_brain.py` — Add logger init + replace all ~43 broad exception handlers with categorized logging

## Handler Inventory (Complete Reference)

Total: ~44 handlers in file. 1 already has logging (line 541). ~43 need conversion.

**DEBUG level** (~25 handlers):
- Lines 635, 642, 661, 668 — ChatWatcher tree walk control access
- Lines 909, 916 — PermissionWatcher tree walk
- Lines 969, 976 — Permission dialog control read
- Lines 997, 1004 — Permission scan walk
- Lines 1021 — Bounding rect in scan
- Lines 1400, 1407 — _find_chat_input walk
- Lines 1111, 1132 — VS Code window SetFocus
- Lines 1544, 1564 — Window activation
- Lines 1555, 1572 — Clipboard save/restore
- Lines 1335, 1342 — VS Code window discovery
- Lines 1371 — Active window tracking
- Lines 296 — Queue drain
- Lines 1511 — Chat input coord caching
- Lines 1936, 1980 — Writer cleanup in finally

**WARNING level** (~8 handlers):
- Lines 45, 63 — Import guards (uiautomation)
- Lines 247 — Voice writer send failure
- Lines 265 — Mobile WebSocket send failure
- Lines 761 — Response extraction retry
- Lines 826, 1260 — Polling recovery (re-find webview)
- Lines 981 — Permission scan fallback
- Lines 1119 — Permission button click
- Lines 1523 — Window cache validation

**ERROR level** (~5 handlers):
- Line 1166 — Prompt input handling
- Line 1474 — Companion extension communication
- Line 1594 — Submit worker execution
- Line 1633 — Voice reader message processing
- Line 1659 — Mobile WebSocket client handler
- Line 1930 — Hook handler execution

**Already handled** (1):
- Line 541 — Command handler execution (uses `logging.exception()`, convert to `log.exception()`)

**Special case** (line 752):
- Bounding rectangle cache — currently prints; convert to `log.debug()` (tolerant polling operation)
