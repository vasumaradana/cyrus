# Implementation Plan: Fix verification failures: 017-Add-permission-approval-logging

**Issue**: [054](/home/daniel/Projects/barf/cyrus/issues/054.md)
**Created**: 2026-03-18
**PROMPT**: prompts/PROMPT_plan.md

## Gap Analysis
**Already exists**:
- Virtual environment at `cyrus2/.venv/bin/python` exists and is functional
- All 761 tests pass (`TEST_COMMAND` succeeds)
- The venv has ruff and pytest installed

**Needs fixing**:
- `BUILD_COMMAND` fails: 2 ruff E501 (line too long >88 chars) errors:
  - `cyrus2/cyrus_common.py:148` (91 chars) — comment line
  - `cyrus2/tests/test_030_headless_mode.py:227` (89 chars) — docstring line
- `CHECK_COMMAND` fails: 1 file needs reformatting (`cyrus2/tests/test_030_headless_mode.py`)

## Approach
Fix the two line-too-long violations and run `ruff format` on the affected file. These are trivial text wrapping fixes. The original "not found" error is resolved (venv exists now).

## Rules to Follow
- No `.claude/rules/` files exist in this project

## Skills & Agents to Use
| Task | Skill/Agent | Purpose |
|------|-------------|---------|
| Fix lint errors | `python-linting` skill | Ruff line-length compliance |

## Prioritized Tasks
- [x] Fix E501 in `cyrus2/cyrus_common.py:148` — wrap comment to ≤88 chars (already fixed)
- [x] Fix E501 in `cyrus2/tests/test_030_headless_mode.py:227` — wrap docstring to ≤88 chars (already fixed)
- [x] Run `ruff format` on `cyrus2/tests/test_030_headless_mode.py` to fix formatting (already fixed)
- [x] Verify BUILD_COMMAND passes — `ruff check`: All checks passed!
- [x] Verify CHECK_COMMAND passes — `ruff format --check`: 38 files already formatted
- [x] Verify TEST_COMMAND still passes — 761 passed, 25 subtests passed

## Acceptance-Driven Tests
| Acceptance Criterion | Required Test | Type |
|---------------------|---------------|------|
| BUILD_COMMAND passes | `cyrus2/.venv/bin/python -m ruff check cyrus2/` exits 0 | lint |
| CHECK_COMMAND passes | `cyrus2/.venv/bin/python -m ruff format --check cyrus2/` exits 0 | format |
| TEST_COMMAND passes | `cyrus2/.venv/bin/python -m pytest cyrus2/tests/` exits 0 | test |

**No cheating** - cannot claim done without all three commands passing.

## Validation (Backpressure)
- Tests: 761 tests must continue passing
- Lint: `ruff check` must exit 0
- Build: `ruff format --check` must exit 0

## Files to Create/Modify
- `cyrus2/cyrus_common.py` — wrap comment on line 148
- `cyrus2/tests/test_030_headless_mode.py` — wrap docstring on line 227 + autoformat
